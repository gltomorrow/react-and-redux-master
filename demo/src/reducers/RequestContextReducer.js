/**
 * This reducer is used to create a placeholder for RequestContext data in Redux.
 * This portion of the state is read only, and is initialized in the initial state.
 * See InitialState.js for implementation.
 */
export function requestContext(state = {}, action) {
    return state;
}
